    <div class="card"><div class="setting-row"><div><b>軽量バックアップ</b><div class="tiny muted">人物・関係・予定・制作・画像の登録情報を保存。画像本体は除外するのでAndroidでも軽い。</div></div><button class="gold-btn" id="exportBtn">軽量保存</button></div><div class="setting-row"><div><b>画像込み完全バックアップ</b><div class="tiny muted">登録画像そのものも含めるため、画像数が多いと端末によっては失敗する場合があります。</div></div><button class="ghost-btn" id="exportFullBtn">完全保存</button></div><div class="setting-row"><div><b>バックアップを読み込む</b><div class="tiny muted">軽量・完全のどちらも読み込みできます。</div></div><button class="ghost-btn" id="importBtn">読込</button></div><div class="setting-row"><div><b>ゴミ箱</b><div class="tiny muted">削除後30日以内は復元可能</div></div><span class="badge">${trash.length}件</span></div></div>
    <div class="section-title"><h2>最近の変更履歴</h2></div>${history.length?history.map(h=>`<div class="card"><b>${esc(h.action)}</b><div class="tiny muted">${fmtDateTime(h.changedAt)} ・ ${esc(h.detail||h.targetType)}</div></div>`).join(''):`<div class="empty">履歴はまだありません</div>`}
    ${trash.length?`<div class="section-title"><h2>ゴミ箱</h2></div>${trash.map(m=>`<div class="card row between"><span>${esc(m.name)}</span><button class="ghost-btn" data-restore="${m.id}">復元</button></div>`).join('')}`:''}
    <div class="section-title"><h2>アプリ情報</h2></div><div class="card tiny muted">MOA WORLD PWA v9.1<br>端末内保存 / オフライン対応 / ID固定方式</div>`;
  document.getElementById('exportBtn').onclick=()=>exportData(false);document.getElementById('exportFullBtn').onclick=()=>exportData(true);document.getElementById('importBtn').onclick=()=>importFile.click();document.querySelectorAll('[data-restore]').forEach(b=>b.onclick=async()=>{const m=await getOne('members',b.dataset.restore);m.deletedAt=null;m.updatedAt=now();await put('members',m);await log('復元','member',m.id,`${m.name}を復元`);toast('復元しました');renderSettings()});
}

function bindCommon(){document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>openAction(b.dataset.action));}
function openAddForCurrentView(){if(state.view==='members')return openAction('member-add');if(state.view==='schedule')return openAction('schedule-add');if(state.memberId)return openAction('image-add');openAction('member-add')}
function openAction(action){if(action==='member-add')return memberModal();if(action==='member-edit')return memberModal(state.memberId);if(action==='image-add')return imageModal(state.memberId);if(action==='schedule-add')return scheduleModal(state.memberId);if(action==='relation-add')return relationModal(state.memberId);if(action==='production-edit')return productionModal(state.memberId)}
function showModal(title,body){modalRoot.innerHTML=`<div class="modal-backdrop"><section class="modal"><div class="modal-head"><h2>${esc(title)}</h2><button class="modal-close" aria-label="閉じる">×</button></div>${body}</section></div>`;modalRoot.querySelector('.modal-close').onclick=closeModal;modalRoot.querySelector('.modal-backdrop').addEventListener('click',e=>{if(e.target.classList.contains('modal-backdrop'))closeModal()})}
function closeModal(){modalRoot.innerHTML=''}

async function memberModal(id=null){const m=id?await getOne('members',id):null;showModal(id?'メンバー編集':'新規メンバー',`<form id="memberForm" class="form"><div class="field-row"><div class="field"><label>名前 *</label><input name="name" required value="${esc(m?.name||'')}"></div><div class="field"><label>かな</label><input name="kana" value="${esc(m?.kana||'')}"></div></div><div class="field-row"><div class="field"><label>年齢</label><input name="age" type="number" min="18" value="${esc(m?.age||'')}"></div><div class="field"><label>ステータス</label><select name="status"><option ${m?.status==='確定'?'selected':''}>確定</option><option ${m?.status==='候補'?'selected':''}>候補</option></select></div></div><div class="field"><label>職業</label><input name="job" value="${esc(m?.job||'')}"></div><div class="field"><label>所属</label><input name="affiliation" value="${esc(m?.affiliation||'')}"></div><div class="field"><label>タグ</label><input name="tags" value="${esc(m?.tags||'')}"></div><div class="field"><label>性格</label><textarea name="personality">${esc(m?.personality||'')}</textarea></div><div class="field"><label>趣味</label><input name="hobbies" value="${esc(m?.hobbies||'')}"></div><div class="field"><label>メモ</label><textarea name="memo">${esc(m?.memo||'')}</textarea></div><label class="row"><input type="checkbox" name="favorite" ${m?.favorite?'checked':''}> 重要・お気に入り</label><button class="gold-btn" type="submit">保存</button>${id?`<button class="danger-btn" type="button" id="softDeleteMember">ゴミ箱へ</button>`:''}</form>`);
  document.getElementById('memberForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const obj={...(m||{}),id:m?.id||uid('mem'),name:f.get('name'),kana:f.get('kana'),age:Number(f.get('age'))||null,status:f.get('status'),job:f.get('job'),affiliation:f.get('affiliation'),tags:f.get('tags'),personality:f.get('personality'),hobbies:f.get('hobbies'),memo:f.get('memo'),favorite:f.get('favorite')==='on',createdAt:m?.createdAt||now(),updatedAt:now(),deletedAt:null};await put('members',obj);await log(id?'編集':'追加','member',obj.id,`${obj.name}を${id?'編集':'追加'}`);closeModal();toast('保存しました');state.memberId=id?obj.id:null;render()};
  if(id)document.getElementById('softDeleteMember').onclick=async()=>{const obj=await getOne('members',id);obj.deletedAt=now();await put('members',obj);await log('削除','member',id,`${obj.name}をゴミ箱へ移動`);closeModal();state.memberId=null;setView('members');toast('ゴミ箱へ移動しました')};
}

async function imageModal(memberId){const members=await activeMembers();showModal('画像を追加',`<form id="imageForm" class="form"><div class="field"><label>人物</label><select name="memberId" required>${members.map(m=>`<option value="${m.id}" ${m.id===memberId?'selected':''}>${esc(m.name)}</option>`).join('')}</select></div><div class="field"><label>画像 *</label><input name="file" type="file" accept="image/*" required></div><div class="field"><label>カテゴリ</label><select name="category" id="imageCategory">${['顔8方向','体型8方向',...ALBUMS].map(x=>`<option>${x}</option>`).join('')}</select></div><div class="field" id="angleField"><label>角度</label><select name="angle">${FACE_ANGLES.map(x=>`<option>${x}</option>`).join('')}</select></div><div class="field-row"><div class="field"><label>状態</label><select name="status">${IMAGE_STATUS.map(x=>`<option>${x}</option>`).join('')}</select></div><div class="field"><label>生成基準の強さ</label><select name="basis">${BASIS.map(x=>`<option>${x}</option>`).join('')}</select></div></div><div class="field"><label>評価メモ</label><textarea name="memo" placeholder="本人固定・角度・髪・体型など"></textarea></div><button class="gold-btn" type="submit">画像を保存</button></form>`);
  const cat=document.getElementById('imageCategory'),angle=document.getElementById('angleField');cat.onchange=()=>angle.style.display=['顔8方向','体型8方向'].includes(cat.value)?'grid':'none';
  document.getElementById('imageForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const file=f.get('file');const dataUrl=await fileToDataURL(file);const category=f.get('category');const obj={id:uid('img'),memberId:f.get('memberId'),category,angle:['顔8方向','体型8方向'].includes(category)?f.get('angle'):'',status:f.get('status'),basis:f.get('basis'),memo:f.get('memo'),dataUrl,createdAt:now(),updatedAt:now(),deletedAt:null};await put('images',obj);await log('画像追加','image',obj.id,`${category} ${obj.angle||''}`);closeModal();toast('画像を保存しました');render()};
}
function fileToDataURL(file){return new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(r.result);r.onerror=()=>rej(r.error);r.readAsDataURL(file)})}

